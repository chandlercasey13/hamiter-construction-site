import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.aplaceformom.com', 'cdn.brookfieldresidential.net'],  // Add the domain of the external image
  },
};

export default nextConfig;
